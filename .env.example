# Host interface to bind. Use 127.0.0.1 behind a reverse proxy (recommended).
# Use 0.0.0.0 only if you want the app reachable directly on the LAN/internet.
APP_BIND=127.0.0.1

# Host port published by Docker.
APP_PORT=8000

TZ=America/New_York
