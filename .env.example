# Host interface to bind. 0.0.0.0 exposes the app on the LAN/internet.
# Use 127.0.0.1 if you put a reverse proxy (Caddy/nginx) in front.
APP_BIND=0.0.0.0

# Host port published by Docker.
APP_PORT=8000

TZ=America/New_York
